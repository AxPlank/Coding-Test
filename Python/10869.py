A, B = map(int, input().split())
print(A + B)
print(A - B)
print(A * B)
print(A // B)
print(A % B)

"""
과정 및 함수 설명
함수

과정
1. input을 통해 값을 입력받는다. split을 통해 input값을 리스트로 변환하고, 변환된 값들을 map을 통해 정수로 변환한다.
2. 덧셈, 뺄셈, 곱셈, 몫, 나머지 결과를 출력한다.
"""